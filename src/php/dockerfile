# Use the official PHP 8.2 image as the base image
FROM php:8.2-fpm

# Install system dependencies and the mysqli extension
RUN apt-get update && apt-get install -y libmysqlclient-dev \
    && docker-php-ext-install mysqli